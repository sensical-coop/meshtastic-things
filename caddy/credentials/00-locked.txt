# Locked placeholder - do not edit this file.
#
# Every admin UI is behind HTTP basic auth, and Caddy needs at least one valid
# credential line or it refuses to start (taking the API down with it). This is
# a bcrypt hash of a long random string that was generated and thrown away, so
# it is a *valid* credential nobody can use.
#
# Add your own credential as a separate file in this directory - it is read
# alongside this one, not instead of it:
#
#   ./scripts/caddy-admin-user.sh alice
#
locked-placeholder $2a$14$/lxOB2MQBLTpYaBfUtwidOvyuv89NQSGW76cEuYNMlMi9xZNsLKZi
